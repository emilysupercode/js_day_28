const text = "It is a truth universally acknowledged, that a single man in possession of a good fortune, must be in want of a wife."
console.log(text);

const universallyIndex = text.indexOf("universally");
console.log(universallyIndex);

const goodIndex = text.indexOf("good");
console.log(goodIndex);

const wifeIndex = text.indexOf("wife");
console.log(wifeIndex);